
import auth from './controllers/auth.controller';
import historic from './controllers/historic.controller';
import hero from './controllers/hero.controller';

const api = {
  auth,
  historic,
  hero
};

export default api;
